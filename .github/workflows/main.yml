name: Build EXE
on: [push]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - run: npm install
      - name: Build with PKG
        run: npx pkg ./src/hello.ts --targets node18-win-x64 --output MyBot.exe
      - name: Upload EXE
        uses: actions/upload-artifact@v3
        with:
          name: MyBot-Exe
          path: MyBot.exe
